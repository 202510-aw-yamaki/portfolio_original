# 作業前に必ず確認

- 文字化けは PowerShell 由来であり、ユーザー側の表示は正常である。
- 文字化けを検知しても、まず PowerShell 側の表示問題として扱い、即時に修正対象へしない。
- 今後インシデントが発生し対応した場合は、このファイルに追記して履歴を残す。

## インシデント履歴

- 2026-03-17: プロフィールトップページのみ左右余白の基準が他ページとずれていたため、[yh_portfolio/css/profile.css](/c:/academia/src/portfolio_original/yh_portfolio/css/profile.css) のメイン領域幅とデスクトップ・タブレット時の余白を他ページ基準へ調整。
- 2026-03-17: 上記だけでは見た目差分が出なかったため再確認。原因は [yh_portfolio/profile.html](/c:/academia/src/portfolio_original/yh_portfolio/profile.html) 下部スクリプトが `.donut-area` 実寸に合わせてドーナツを最大化し、[yh_portfolio/css/profile.css](/c:/academia/src/portfolio_original/yh_portfolio/css/profile.css) 側に左右カラムが無かったこと。タブレット以上で中央カラム固定の3カラム構成へ修正。
- 2026-03-17: さらに再確認。ヘッダーとフッターの幅・角丸が崩れていた直接原因は、[yh_portfolio/css/profile.css](/c:/academia/src/portfolio_original/yh_portfolio/css/profile.css) が `common.css` の `.stage/.site` にある `width: min(1440px, 100vw)` と角丸基準をプロフィールページだけ上書きしていたこと。モバイル以外では `.stage` 上書きを除去し、他ページと同じ外枠基準へ戻した。
- 2026-03-17: Chrome で `file://` から [profile.html](/c:/academia/src/portfolio_original/yh_portfolio/profile.html) を直接開いた際、`fetch('content.json')` が CORS 制約で失敗する事象を確認。Live Preview や GitHub Pages のように `http/https` で配信される環境では再現しないため、ローカル直開き特有の挙動として扱う。エラー例: `Access to fetch at 'file:///.../content.json' from origin 'null' has been blocked by CORS policy`、`net::ERR_FAILED`。
