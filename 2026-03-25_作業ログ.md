# 2026-03-25 作業ログ

- 作成日時: 2026-03-25 16:19:19
- 作業対象: `yh_portfolio`

## 16:19 時点まとめ

### 1. 現状確認
- リポジトリ直下と `yh_portfolio` 配下の構成を確認。
- `profile.html` / `portfolio.html` / `skills.html` / `contact.html` / `content_editor.html` / 各CSS / `content.json` の内容を確認。
- PowerShell 上で `README.md`、`作業前に必ず確認.md`、`content.json` に文字化け表示があることを確認。

### 2. プロフィールページ
- SP幅 `767px` 以下で `クリックして選択` を非表示化。
- 追加で画面高さ `820px` 以下でも `クリックして選択` を非表示化。
- 途中で中央円サイズ計算の変更を一度入れたが、意図確認前の変更だったため元に戻した。

### 3. スキルページ
- `board-inner` に `min-height: 300px` を設定。
- `area cert` / `area tool` の縦スクロールバー色を白寄りに調整し、視認性を上げた。

### 4. お問い合わせページ
- `scene` に `min-height: 300px` を設定。

### 5. 共通レイアウト
- `.stage, .site` に `min-height: 400px` を追加。
- 画面高さ不足時のフッター挙動を見直し、低高さ時はフッターカードを隠してメタ情報中心の軽い表示に切り替えるよう調整。

### 6. フッター改善
- フッター中央要素として `©2026Yobibit` と `ポートフォリオ用テキスト入力エディタ` を中央固定化。
- フッター左側にハンバーガーメニューを追加。
- ハンバーガーメニューは縦積み構成に統一。
- メニュー項目は `プロフィール / ポートフォリオ / スキル / お問合せ` に整理。
- 現在表示中のページはハンバーガーメニュー内で非表示にするよう調整。
- 通常時は既存フッターカード表示、低高さ時のみカード非表示 + ハンバーガーメニュー表示に整理。

### 7. 冗長部分の整理
- フッター周りの `footer-*` と `site__footer-*` の二重クラス運用を整理。
- `common.css` は `site__footer-*` 系に統一。
- 各HTMLのフッタークラス名も `site__footer-*` 系へ統一。
- 既存JSには手を入れず、挙動を変えない範囲で整理。

## 変更対象ファイル
- `yh_portfolio/css/common.css`
- `yh_portfolio/css/profile.css`
- `yh_portfolio/css/skills.css`
- `yh_portfolio/css/contact.css`
- `yh_portfolio/profile.html`
- `yh_portfolio/portfolio.html`
- `yh_portfolio/skills.html`
- `yh_portfolio/contact.html`

## 備考
- ブラウザでの最終表示確認は未実施。
- フッターまわりは共通CSS寄せで整理済みだが、実画面での最終確認は別途必要。
